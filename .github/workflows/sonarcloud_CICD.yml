name: SonarCloud Build

on:
  push:
    branches:
      - main

  pull_request:
    types:
      - opened
      - synchronize
      - reopened

  workflow_dispatch:

jobs:
  sonarqube:
    name: SonarCloud Scan
    runs-on: ubuntu-latest

    steps:
      # Checkout Repository
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # Setup Python
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # Install Dependencies
      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip

          if [ -f requirements.txt ]; then
            pip install -r requirements.txt
          fi

      # SonarCloud Scan
      - name: SonarCloud Scan
        uses: SonarSource/sonarqube-scan-action@v6
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.projectKey=kurapatiharshith_TO-GENERATE-A-SECURE-FRAMEWORK-IN-IDENTITY-MANAGEMENT-USING-BLOCKCHAIN-
            -Dsonar.organization=kurapatiharshith
            -Dsonar.host.url=https://sonarcloud.io
            -Dsonar.sources=.
            -Dsonar.python.version=3.11

      # Install PDF Libraries
      - name: Install PDF Dependencies
        run: |
          pip install requests fpdf

      # Generate PDF Report
      - name: Generate PDF Report
        run: |
          python scripts/generate_sonar_report.py
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

      # Upload PDF Report
      - name: Upload PDF Report
        uses: actions/upload-artifact@v4
        with:
          name: sonarcloud-report
          path: sonar-report.pdf
